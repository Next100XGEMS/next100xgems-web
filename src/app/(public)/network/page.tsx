import PublicPlaceholderPage from "@/components/public/public-placeholder-page";

export default function NetworkPage() {
  return <PublicPlaceholderPage eyebrow="Network" title="A network built around useful context." description="The NEXT100XGEMS network surface will arrive in a later product gate." />;
}
