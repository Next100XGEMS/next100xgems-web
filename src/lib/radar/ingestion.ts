import { freezeRadarInputs } from "@/lib/radar/fast-lane";
import { sha256 } from "@/lib/radar/hash";
import { normalizeProviderObservation } from "@/lib/radar/normalization";
import type { RadarFastLaneMethod, RadarNormalizedObservation, RadarProviderAdapter, RadarProviderRequest } from "@/lib/radar/contracts";
import type { RadarSystemGateway } from "@/lib/radar/system-gateway";
export type RadarIngestionInput = { eventKey: string; eventType: string; tokenId: string | null; sourceEventId: string | null; observedAt: string; context?: Record<string, unknown>; requests: readonly (RadarProviderRequest & { adapter: RadarProviderAdapter })[]; method: RadarFastLaneMethod };
export type RadarIngestionReceipt = { eventId: string; observationIds: readonly string[]; workItemId: string; observations: readonly RadarNormalizedObservation[] };
export async function ingestRadarEvent(input: RadarIngestionInput, gateway?: RadarSystemGateway): Promise<RadarIngestionReceipt> { const active = gateway ?? (await import("@/lib/radar/system-gateway")).createRadarSystemGateway(); const observations = await Promise.all(input.requests.map(async (request) => normalizeProviderObservation(await request.adapter.observe(request)))); const frozen = freezeRadarInputs(observations, input.observedAt); const payloadHash = sha256({ eventKey: input.eventKey, eventType: input.eventType, tokenId: input.tokenId, sourceEventId: input.sourceEventId, observedAt: input.observedAt, observations: frozen.observations }); const eventId = await active.insertEvent({ eventKey: input.eventKey, eventType: input.eventType, tokenId: input.tokenId, sourceProvider: observations[0]?.provider ?? "unknown", sourceEventId: input.sourceEventId, payloadHash, context: input.context ?? {}, observedAt: input.observedAt }); const observationIds: string[] = []; for (const observation of observations) observationIds.push(await active.recordObservation({ eventId, tokenId: input.tokenId, observation })); const workItemId = await active.enqueueWork({ requestKey: `screening:${input.eventKey}:${input.method.version}:${frozen.inputHash}`, workKind: "SCREENING", tokenId: input.tokenId, eventId, parentAnalysisId: null, methodVersion: input.method.version, inputVersion: input.method.inputVersion }); for (const observationId of observationIds) await active.attachObservation(workItemId, observationId); return { eventId, observationIds, workItemId, observations }; }
