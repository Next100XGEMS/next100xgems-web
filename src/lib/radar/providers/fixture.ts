import type { RadarProviderAdapter, RadarProviderCapability, RadarProviderObservation, RadarProviderRequest } from "@/lib/radar/contracts";
export type FixtureProviderReading = Omit<RadarProviderObservation, "provider" | "adapterVersion" | "capability" | "metricKey" | "observedAt"> & { capability: string; metricKey: string; observedAt?: string };
const capabilities: readonly RadarProviderCapability[] = [{ capability: "market", metrics: ["liquidity", "volume", "market_cap"], supportsHistorical: false }, { capability: "contract", metrics: ["authority_risk"], supportsHistorical: false }];
export class FixtureRadarProvider implements RadarProviderAdapter { readonly provider = "fixture"; readonly adapterVersion = "fixture-v1"; readonly capabilities = capabilities; private readonly readings: ReadonlyMap<string, FixtureProviderReading>; constructor(readings: readonly FixtureProviderReading[] = []) { this.readings = new Map(readings.map((item) => [`${item.capability}:${item.metricKey}`, item])); } async observe(request: RadarProviderRequest): Promise<RadarProviderObservation> { const item = this.readings.get(`${request.capability}:${request.metricKey}`); return item ? { ...item, provider: this.provider, adapterVersion: this.adapterVersion, capability: request.capability, metricKey: request.metricKey, observedAt: item.observedAt ?? request.observedAt } : { provider: this.provider, adapterVersion: this.adapterVersion, capability: request.capability, metricKey: request.metricKey, state: "UNKNOWN", reason: "fixture reading is not configured", observedAt: request.observedAt }; } }
