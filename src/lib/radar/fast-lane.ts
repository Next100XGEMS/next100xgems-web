import { compareDecimals, normalizeDecimal } from "@/lib/radar/decimal";
import { sha256 } from "@/lib/radar/hash";
import type { RadarFastLaneMethod, RadarFastLaneResult, RadarFastLaneRule, RadarFrozenInputs, RadarNormalizedObservation } from "@/lib/radar/contracts";
function matches(value: string, rule: RadarFastLaneRule) { if (!rule.operator || rule.threshold === undefined) return true; const c = compareDecimals(value, normalizeDecimal(rule.threshold)); return rule.operator === "gt" ? c > 0 : rule.operator === "gte" ? c >= 0 : rule.operator === "lt" ? c < 0 : c <= 0; }
export function freezeRadarInputs(observations: readonly RadarNormalizedObservation[], sealedAt = new Date().toISOString()): RadarFrozenInputs { const ordered = [...observations].sort((left, right) => `${left.provider}:${left.capability}:${left.metricKey}:${left.observedAt}:${left.contentHash}`.localeCompare(`${right.provider}:${right.capability}:${right.metricKey}:${right.observedAt}:${right.contentHash}`)); return { schemaVersion: "frozen-input-v1", observations: ordered, sealedAt, inputHash: sha256({ schemaVersion: "frozen-input-v1", observations: ordered }) }; }
export function evaluateFastLane(method: RadarFastLaneMethod, observations: readonly RadarNormalizedObservation[], evaluatedAt = new Date().toISOString()): RadarFastLaneResult { const frozen = freezeRadarInputs(observations, evaluatedAt); const byMetric = new Map(frozen.observations.map((item) => [`${item.capability}:${item.metricKey}`, item])); const reasons: RadarFastLaneResult["reasons"][number][] = []; const evidence: RadarFastLaneResult["evidence"][number][] = []; for (const rule of method.rules) { const observation = byMetric.get(`${rule.capability}:${rule.metricKey}`); if (!observation || observation.state !== "AVAILABLE" || observation.value === null) { const detail = observation ? `${rule.metricKey} is ${observation.state.toLowerCase()}.` : `${rule.metricKey} was not supplied by the provider.`; evidence.push({ key: rule.key, label: rule.key, state: "UNKNOWN", origin: "DETERMINISTIC", metricKey: rule.metricKey, value: null, reason: detail }); if (rule.required) reasons.push({ code: `DATA_${observation?.state ?? "UNKNOWN"}`, ruleKey: rule.key, detail }); continue; } if (!matches(observation.value, rule)) { const detail = rule.rejectReason ?? `${rule.metricKey} failed the versioned Fast Lane rule.`; reasons.push({ code: "RULE_REJECTED", ruleKey: rule.key, detail }); evidence.push({ key: rule.key, label: rule.key, state: "STRONG_SIGNAL", origin: "DETERMINISTIC", metricKey: rule.metricKey, value: observation.value, reason: detail }); } else evidence.push({ key: rule.key, label: rule.key, state: "VERIFIED_DATA", origin: "DETERMINISTIC", metricKey: rule.metricKey, value: observation.value }); } const reject = reasons.some((item) => item.code === "RULE_REJECTED"); return { result: reject ? "REJECT" : reasons.length ? "INCOMPLETE" : "PASS", methodVersion: method.version, inputVersion: method.inputVersion, inputHash: frozen.inputHash, evaluatedAt, reasons, evidence }; }
export function createUnconfiguredScoreCalculator(): never { throw new Error("No production Radar scoring methodology has been approved."); }
